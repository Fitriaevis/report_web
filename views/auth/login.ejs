<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - PLN UP3 Gresik</title>
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/temp_report_web/assets/favicon.ico" />
    <!-- Custom Google font -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600&display=swap" rel="stylesheet" />
    <!-- Bootstrap icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css" rel="stylesheet" />
    <!-- Core theme CSS (includes Bootstrap) -->
    <link href="/temp_report_web/css/styles.css" rel="stylesheet" />
    <style>
        body {
            background: linear-gradient(to right, #002c77, #088a9b);
            color: #fff;
            font-family: 'Plus Jakarta Sans', sans-serif;
        }
        .login-container {
            background: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            padding: 20px;
            max-width: 600px;
            margin: 50px auto;
        }
        .login-container h1 {
            color: #0056a1;
            margin-bottom: 20px;
        }
        .login-form {
            margin-top: 20px;
        }
        .login-container .form-control {
            border-radius: 0.25rem;
        }
        .login-container .btn-primary {
            background-color: #0056a1;
            border-color: #0056a1;
        }
        .login-container .btn-primary:hover {
            background-color: #003d6b;
            border-color: #003d6b;
        }
        .login-container .register-link {
            margin-top: 20px;
        }
        .login-container .register-link a {
            color: #0056a1;
            text-decoration: none;
        }
        .login-container .register-link a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="login-container text-center">
            <h1 class="display-4 fw-bold">Login</h1>
            <div class="login-form">
                <!-- proses login email/password tidak sesuai -->
                <form action="/log" method="POST">
                    <div class="input-group mb-3">
                        <span class="input-group-text">@</span>
                        <div class="form-floating">
                            <input type="email" name="email" class="form-control" id="floatingInputGroup1" placeholder="Email" autocomplete="email">
                            <label for="floatingInputGroup1" class="text-black">Email</label>
                        </div>
                    </div>
                    <div class="input-group mb-3">
                        <span class="input-group-text"><i class="bi bi-key-fill"></i></span>
                        <div class="form-floating">
                            <input type="password" name="password" class="form-control" id="floatingPassword" placeholder="Password" autocomplete="current-password">
                            <label for="floatingPassword" class="text-black">Password</label>
                        </div>
                    </div>
                    <button type="submit" class="btn btn-primary">Login</button>
                    <div class="register-link mt-3 text-black">Don't have an account?
                        <a href="/register">Create!</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
